server:
  port: 8005
spring:
  cloud:
    server:
      git:
        default-label: development
        uri: https://github.com/mcorverasoft/config-server
        search-paths: config
  application:
    name: config-service